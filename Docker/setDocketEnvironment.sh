# Before you use docker or docker-compose on your Mac, you typically use the command:
# eval $(docker-machine env default)
#  to set environment variables so that docker or docker-compose know how to talk to 
# Docker Engine running on VirtualBox.
eval $(docker-machine env default)

